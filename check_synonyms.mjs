import { createClient } from "@supabase/supabase-js"; import dotenv from "dotenv"; dotenv.config({path: ".env.local"}); const supa = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY); async function run() { const {data, error} = await supa.from("words_master").select("word, synonyms").not("synonyms", "is", null).neq("synonyms", "").limit(5); console.log("Data:", data, "Error:", error); } run();